# ProPR Web UI

Static web UI for operating ProPR. It provides the browser interface for
repositories, tasks, plans, logs, settings, and agent activity.

Most users should start ProPR with `propr/launcher`, which runs this image with
the backend service and supporting containers.

```bash
docker pull propr/ui:latest
```

Source: https://github.com/integry/propr
