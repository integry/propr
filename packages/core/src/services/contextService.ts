/**
 * Context service - facade module that re-exports from the context/ subdirectory.
 * This maintains backwards compatibility with existing imports.
 */

// Re-export everything from the context module
export * from './context/index.js';
