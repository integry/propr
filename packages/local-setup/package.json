{
  "name": "@propr/local-setup",
  "version": "0.8.15",
  "description": "UI-agnostic local ProPR setup state machine",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "npx tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@propr/shared": "^0.8.15"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3"
  }
}
